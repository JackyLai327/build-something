import NavBar from "./NavBar";
import SoundButton from "./SoundButton";

const UIControls = () => {
    return (
    <div>
        <NavBar />
        <SoundButton />
    </div>
    );
}

export default UIControls;